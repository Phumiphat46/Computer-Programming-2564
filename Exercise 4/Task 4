"""ข้อ 4 จงเขียนโปรแกรมรับค่าตัวเลข 1 ตัว (n) จากคีย์บอร์ด จากนั้นแสดงผลตัวเลขตั้งแต่ 1 – n ในบรรทัด
เดียวกันด้วยคำสั่ง while loop ดังแสดงในตัวอย่าง
"""
Number = int(input("enter number : "))
i = 1
while Number > 1:
  print ( i ,end = " ")
  if i == Number:
    break
  i+=1
